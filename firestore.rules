rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // 只允许认证用户访问自己的 user 文档
    match /users/{userId} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
    // 其他集合（如公开内容）可单独加规则
    // match /public/{docId} {
    //   allow read: if true;
    //   allow write: if request.auth != null;
    // }
  }
}